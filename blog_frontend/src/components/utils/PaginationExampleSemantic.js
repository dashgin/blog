import React from 'react'
import { Pagination } from 'semantic-ui-react'

const PaginationExampleSemantic = (defaultActivePage, totalPages) => (
    <Pagination defaultActivePage={defaultActivePage} totalPages={totalPages} />
)

export default PaginationExampleSemantic
